namespace Utils;

public class Calculator
{
    private Calculator()
    {
    }

    // public static Bro BroadcastAddress(InterfaceAddress interfaceAddress, NetMask netMask)
    // {
    //     return new BroadcastAddress(interfaceAddress, netMask);
    // }

    // public static IPv4Address NetAddress(IPv4Address interfaceAddress, IPv4Address netMask)
    // {
    //     return new IPv4Address(interfaceAddress.Address & netMask.Address);
    // }

    // public static int NumberOfHosts()

    public record Config
    {
    }
}